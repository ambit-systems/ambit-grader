__pycache__/
*.pyc
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Environments and caches. Absent from the sibling repos' ignore files, where a
# virtualenv stays out of git only because nobody runs `git add -A`.
.venv/
.uv-cache/

# macOS
.DS_Store
